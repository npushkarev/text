G=~/grpc.lin.gcc84.shared.x86_64.1.60.1.1/lib/native/lin-gcc84-shared-x86_64
  cd "$G"
  cp -n libgrpc.a libgrpc.a.bak
  ld -r --whole-archive \
     libgrpc.a libupb_collections_lib.a libupb_json_lib.a libupb_textformat_lib.a \
     --no-whole-archive -z muldefs \
     -o /tmp/grpc_full.o
  rm -f libgrpc.a && ar qcs libgrpc.a /tmp/grpc_full.o
  # пустая libupb.a — чтобы линкер её нашёл по пути, но она ничего не добавляла:
  : | gcc -x c -c -o /tmp/empty.o - && rm -f libupb.a && ar rcs libupb.a /tmp/empty.o
  cd ~/grpc_sdk/.build/lin.gcc.shared.x64 && cmake --build . --target grpc_sdk_test
