C=$(grep -rl "IW Transparent" /usr/local/share/ca-certificates/ /etc/ssl/certs/ 2>/dev/null | head -1)
[ -z "$C" ] && C=$(ls /etc/ssl/certs/*.pem 2>/dev/null | head -1)
cp "$C" iw-proxy-root.crt && echo "взят: $C"
